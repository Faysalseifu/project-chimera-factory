# Skill: Generate Consistent Image

## Purpose
Generate persona-consistent images for campaigns using a stable character reference.

## Input JSON Schema
```json
{
  "prompt": "string",
  "character_reference_id": "string",
  "style": "string",
  "aspect_ratio": "string"
}
```

## Output JSON Schema
```json
{
  "image_url": "string",
  "provider": "string",
  "content_hash": "string"
}
```

Implementation later – this is contract only.
